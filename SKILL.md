---
name: qa-test-case-generator
slug: qa-test-case-generator
displayName: Test Case Generator
version: 1.7.7
description: >-
  一键从需求生成结构化测试用例的整合技能。把需求评审、需求解构、场景树、边界分析、组合策略、
  状态转换、风险识别、功能类型启发式清单、接口/移动端/Agent/性能/安全专项、AI输出评审与盲区补盲、
  防幻觉校验与覆盖率报告整合为一条六步流水线。输出标准 9 列测试用例 CSV（P0-P3 分级、需求可追溯、
  覆盖率标注口径）。适用于：上传 PRD/Word/PDF/URL 或一句话需求要完整测试用例时；担心遗漏边界/异常/
  并发/时序场景时；需要 AI 评审测试输出并补盲时。
when_to_use: >-
  用户说"生成测试用例"、"帮我测试"、"设计测试"、"上传需求"、"开始测试"、"需求评审"、"边界分析"、
  "补盲"、上传 PRD/需求文档/URL 时自动激活。也适用于单独生成某模块用例或评审已有用例。
allowed-tools: Read Grep Glob WebFetch Bash
related_skills:
  all_skills:
    - qa-test-skills
input_format:
  required:
    - name: 用户需求
      type: string
      description: 需求描述，可为文字、文件路径或 URL
  optional:
    - name: 需求文档
      type: file
      description: PRD / Word / PDF / Markdown
    - name: URL
      type: string
      description: 需求文档链接
output_format:
  structure:
    - test_cases: "标准 9 列测试用例 CSV"
    - coverage_report: "覆盖率报告（标注口径）"
    - risk_areas: "高风险区域"
    - test_report: "测试报告.md"
  traceability:
    - 用例ID：TC_{模块缩写}_{功能缩写}_{序号}
    - 需求ID：REQ-{模块缩写}-{序号}
    - 风险ID：RISK-{模块缩写}-{类型}-{序号}
categories: ['Development','Testing']
depth_requirement_quantification:
  reference_value: "用例总数 = 需求点 × 3/5/7（简单/中等/复杂）"
  minimum: "用例总数不低于需求点的 3 倍"
error_recovery_guidance:
  on_failure: "遗漏维度时回退到对应分析步骤补充后重新生成"
  retry_behavior: "补全分析后重新执行第 4-6 步"
---

# 测试用例生成器

> 默认**不读取代码实现**，用例验证"系统应该做什么"，而非"如何实现"。

## 一、硬约束（始终生效）

1. **直接产出**：必须在本次响应直接输出用例，不得只承诺"我将生成"；数据不足时先输出已有分析，再标注缺什么。
2. **覆盖诚实**：覆盖率必须写"基于现有需求文档的覆盖率"，禁止"全覆盖/100%"；缺失模块标"未覆盖+原因+建议补充"，**不得编造需求或用例**。
3. **可追溯**：每条用例带唯一 `TC_{模块缩写}_{功能缩写}_{序号}` 并关联 `REQ-xxx`；风险点带 `RISK-xxx`。
4. **CSV 交付**：最终交付 `test-output/测试用例.csv`（见第六节格式），另附 `test-output/测试报告.md`。

## 二、复杂度判定与深度

先判定复杂度，据此决定挖掘深度与用例规模。

| 维度 | 简单 | 中等 | 复杂 |
|------|------|------|------|
| 模块范围 | 单模块 | 2-5 模块 | 5+ 模块 / 跨系统 |
| 风险 | 低 | 中 | 高（资金/安全/核心链路） |
| 并发 | 无 | 少量 | 高并发 |
| 数据敏感 | 低 | 中 | 高（用户/财务） |
| 外部依赖 | 无/少 | 中 | 多/核心依赖 |

| 产出 | 简单 | 中等 | 复杂 |
|------|------|------|------|
| 需求总数（显性×倍数） | ×2 | ×3 | ×4 |
| 场景数（显性×倍数） | ×3 | ×5 | ×7 |
| 边界数（场景×倍数） | ×1.5 | ×2 | ×2.5 |
| **用例总数（显性×倍数）** | **×3** | **×5** | **×7** |

## 三、六步工作流

> 每步都要产出实际内容（内联分析即可，不必每步单独落盘）；完成后过该步检查点。

### 第 1 步：输入解析与校验
- 识别输入：文字 / 文件（.md/.docx/.pdf/.txt）/ URL / 需求目录；主文档含索引引用时必须读取全部子模块文档并合并。
- 三维校验：需求明确性（功能/目标/角色/成功标准）、上下文充分性（背景/约束/依赖/历史缺陷）、输入类型。
- 评分 <4 或 4-6 时，列出缺失信息 + 具体追问；**先输出已有数据的部分分析**，再标注缺口。

### 第 2 步：需求评审 + 三层解构
- **五维评审**（各 10 分）：完整性、清晰性、一致性、可测试性、可实现性；问题分 P0 阻塞 / P1 重要 / P2 建议，每条 P0/P1 必须带改进建议。
- **三层需求**：显性（文档写明）、隐性（五问法：如果不？异常呢？假设呢？不存在呢？并发呢？）、衍生（换角色/场景/边界/关联功能/数据流向推导）。
- **五维拆解**：输入、操作、状态、输出、规则。
- **业务规则结构化**：`RULE-xxx：若[条件]，则[必须满足]`（计算/校验/权限/流程规则）。
- 产出需求解构表：`需求ID | 显性描述 | 隐性假设 | 风险点 | 疑问`。
- 检查点：隐性需求 ≥3 条；五维齐全；每条需求可测试。

### 第 3 步：分析建模（风险 + 场景 + 边界 + 组合 + 状态）
- **风险雷达（5 信号）**：变更类型、业务影响、技术复杂度、数据风险、集成风险；各按高/中/低判定，风险分 = 业务影响 × 概率 × 技术复杂度。每模块 ≥5 个风险点，高风险区域约 60% 测试资源。
- **启发式清单**：按功能类型套用检查清单（表单/列表/购物车/支付/导入导出/审批/通知/权限），详见 `references/checklists.md`。
- **场景树**：主路径 ×1、分支路径 ×2、异常路径 ×1.5、数据流 ×0.5（相对需求数）；异常必须覆盖输入/状态/网络/依赖/并发/资源。
- **四维边界**：输入边界（值域/格式/组合/业务）、状态边界（转换/值/持续）、时间边界（超时/并发/时序/周期）、资源边界（内存/连接/线程/磁盘/网络）。
- **组合策略**：参数 3-10 个→Pairwise（≤全组合 10-30%）；>10 个→正交表（5-20%）；高风险→全组合；中风险→Pairwise；低风险→单参数。
- **状态转换**：识别状态/转换/路径；四类测试：合法（状态数×2）、非法（×1.5）、临界（每条转换×1）、并发（高并发状态×2）。
- **领域建模**（复杂项目）：状态转换图 / 数据流图 / 服务依赖图，必须输出可测的表而非只画图。
- **批判思维**：对每个"正常"问"异常呢"，对每个"确定"问"什么假设"，至少挑战 3 个隐含假设。
- 检查点：每个高风险点有概率+影响+缓解；边界覆盖 ≥3 个维度；非法状态转换已列。

### 第 4 步：生成测试用例
- 把第 2-3 步的分析结果转成用例，套用设计方法（第四节）与覆盖维度（第五节）。
- 每条用例：只验证一个明确目标；预期结果客观可测、用"应/必须/会"；**测试步骤留空**由用户按实际系统补充（避免幻觉与返工）。
- 按复杂度产出足够数量（第二节倍率），P0-P3 分级并标注风险等级。
- 检查点：主/分支/异常/边界齐全；字段级验证覆盖每个表单字段；预期结果可验证。

### 第 5 步：AI 评审 + 盲区补盲
- **八维评审**（各 10 分，总分 ≥64/80 合格；任一维度 <7 必须列出问题）：完整性、正确性、可执行性、风险覆盖、规范性、追溯性、一致性、冗余度。无上游场景树/风险清单时降级为通用清单检查并标注。
- **六盲区补盲**（每类 ≥2-3 条，共 12-18 条）：时序依赖、并发冲突、资源竞争、状态累积、数据一致性、第三方集成。
- 问题用 `MISSING/WRONG/VAGUE/REDUNDANT/RISK/FORMAT` 标记并给出修正；系统性问题回退第 4 步重生成。
- 检查点：六盲区全覆盖；评审问题已修正。

### 第 6 步：防幻觉校验 + 输出
- **四维校验**：事实核查（需求/风险/边界是否真实存在，查 `REQ/RISK/BD`）、一致性（需求↔用例、风险↔用例、场景↔边界无矛盾）、可执行性（数据可造/环境可搭/结果可测，评分 ≥7 可接受）、来源追溯（每条用例标注需求来源、风险来源、评审状态）。
- 六类幻觉处置：虚无功能→删除或标注；过度推断→降级为假设；数据编造→修正或标注 `[假设]`；时间/数值幻觉→对齐原文；跨模块矛盾→统一；依赖幻觉→标注前置假设。
- 输出：`test-output/测试用例.csv` + `test-output/测试报告.md`（含用例统计、覆盖率[标注口径]、缺口清单、高风险区域、回归分级）。
- 回归分级（复杂项目）：L1 冒烟 P0 <15min / L2 核心 P0+P1 <2h / L3 全量 P0-P3；每个 P0 至少 1 条回归、每个历史缺陷至少 1 条复测。

## 四、设计方法速查（按场景选用）

| 方法 | 适用 | 必用场景 |
|------|------|----------|
| 等价类划分 | 输入域 | 所有输入字段 |
| 边界值分析 | 边界 | 所有有边界限制字段 |
| 场景法 | 业务流程 | 核心业务流程 |
| 判定表驱动 | 多条件组合 | 多条件组合 |
| 错误推测 | 经验驱动 | 异常场景 |
| 因果图 | 复杂条件依赖 | 复杂条件组合 |
| 正交试验 | 多因素多水平 | 多因素兼容性 |
| 状态转换 | 状态机 | 有状态变化功能 |
| 风险驱动 | 高风险优先 | 高风险功能 |

## 五、覆盖维度（逐项自检）

- **功能**：主流程、分支、异常、边界、退出（退出/注销/超时）、状态转换。
- **数据**：有效、无效、边界、特殊字符/超长、格式校验（邮箱/手机/身份证）、注入（SQL/XSS/命令）。
- **字段级**（每个表单字段）：长度边界（1/最大/最大+1）、格式、注入、特殊字符（空格/符号/Unicode）、空值、默认值。
- **权限**：角色权限、越权、数据隔离、功能权限、CSRF、路径遍历。
- **集成**：模块间、接口、数据流转、异常处理。
- **非功能**：性能（响应/吞吐/并发）、兼容性（浏览器/系统/设备）、可用性、安全、可靠性。

## 六、输出契约

**测试用例.csv（固定 9 列，标准 CSV）**

```
用例编号,测试类型,功能模块,测试标题,用例级别,预置条件,测试步骤,预期结果,风险等级
TC_AUTH_LOGIN_001,功能测试,认证,正确凭据登录成功,P0,"已注册用户u1,密码符合规则",访问/login;输入用户名;输入密码;点击登录,跳转首页并显示用户名;Session建立,高
```

- 分隔符半角逗号；字段含逗号/双引号/换行必须用双引号包裹并按 RFC 4180 转义；UTF-8 含 BOM。
- **禁止 `|` 竖线分隔、禁止 Markdown 表格**；表头必须存在；每条用例一行。
- 编号：`TC_{模块缩写}_{功能缩写}_{序号}`；需求 `REQ-模块-序号`；风险 `RISK-模块-类型-序号`；场景 `SC-...`；边界 `BD-...`。
- 测试类型：功能/异常/边界/并发/安全/性能/兼容性/接口。
- 用例级别：P0 关键（≤20%，核心流程 100%）、P1 重要（≤40%）、P2 一般（≤30%）、P3 可选（≤10%）。P0+P1 ≥60%。
- 风险等级：高/中/低。
- 质量线：用例数 ≥ 需求点 ×3；每条平均预期结果 3-5 条；P0 用例 100% 覆盖现有需求。

## 检查清单（交付前自检）

- [ ] 输入校验 / 需求评审 / 三层解构 / 五维拆解完成？
- [ ] 风险雷达、场景树、四维边界、组合策略、状态转换已应用？
- [ ] 用例覆盖主/分支/异常/边界、字段级、权限、集成、非功能？
- [ ] 八维评审 ≥64 分且问题已修正？六盲区各 ≥2-3 条？
- [ ] 四维防幻觉校验通过，无编造需求/数据/时间？
- [ ] CSV 为 9 列标准格式（UTF-8 BOM / RFC 4180 / 无竖线）？
- [ ] 覆盖率标注口径，缺口写明"未覆盖+原因+建议补充"？

## 参考

- `references/checklists.md` — 8 类功能检查清单 + 接口/移动端/Agent/性能/安全专项要点（按需加载）。
